import {scale} from 'react-native-size-matters';

export const STANDARD_FLEX = 1;
export const ICON_SIZE = scale(20);
export const BUTTON_HEIGHT = scale(40);
export const INFO_FONT_SIZE = scale(12);
export const HEADING_FONT_SIZE = scale(16);
export const BUTTON_LABEL_SIZE = scale(12);
